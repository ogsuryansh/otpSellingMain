<!-- Modern Responsive Navbar -->
<nav class="bg-white/95 backdrop-blur-sm shadow-lg border-b border-gray-200 sticky top-0 z-50 navbar-container">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="flex justify-between h-16">
      <!-- Logo and Brand -->
      <div class="flex items-center">
        <div class="flex-shrink-0 flex items-center">
          <div class="w-10 h-10 bg-gradient-to-r from-blue-600 to-purple-600 rounded-xl flex items-center justify-center mr-3 shadow-lg">
            <i class="fas fa-robot text-white text-sm"></i>
          </div>
          <span class="text-xl font-bold bg-gradient-to-r from-blue-600 to-purple-600 bg-clip-text text-transparent">OTP Bot</span>
        </div>
      </div>

      <!-- Desktop Navigation -->
      <div class="hidden md:flex items-center space-x-1">
        <a href="/admin-dashboard" class="nav-link <%= typeof page !== 'undefined' && page === 'dashboard' ? 'active' : '' %>">
          <i class="fas fa-chart-line mr-2"></i>
          Dashboard
        </a>
        <a href="/add-server" class="nav-link <%= typeof page !== 'undefined' && page === 'add-server' ? 'active' : '' %>">
          <i class="fas fa-server mr-2"></i>
          Add Server
        </a>
        <a href="/add-service" class="nav-link <%= typeof page !== 'undefined' && page === 'add-service' ? 'active' : '' %>">
          <i class="fas fa-plus-circle mr-2"></i>
          Add Service
        </a>
        <a href="/connect-api" class="nav-link <%= typeof page !== 'undefined' && page === 'connect-api' ? 'active' : '' %>">
          <i class="fas fa-plug mr-2"></i>
          Connect API
        </a>
        <a href="/my-services" class="nav-link <%= typeof page !== 'undefined' && page === 'my-services' ? 'active' : '' %>">
          <i class="fas fa-list mr-2"></i>
          My Services
        </a>
        <a href="/bot-settings" class="nav-link <%= typeof page !== 'undefined' && page === 'bot-settings' ? 'active' : '' %>">
          <i class="fas fa-cog mr-2"></i>
          Bot Settings
        </a>
        <a href="/qr-code" class="nav-link <%= typeof page !== 'undefined' && page === 'qr-code' ? 'active' : '' %>">
          <i class="fas fa-qrcode mr-2"></i>
          QR Code
        </a>
      </div>

      <!-- Mobile menu button -->
      <div class="md:hidden flex items-center">
        <button id="mobile-menu-button" class="text-gray-600 hover:text-blue-600 focus:outline-none focus:text-blue-600">
          <i class="fas fa-bars text-xl"></i>
        </button>
      </div>
    </div>
  </div>

  <!-- Mobile Navigation -->
  <div id="mobile-menu" class="md:hidden hidden bg-white border-t border-gray-200">
    <div class="px-2 pt-2 pb-3 space-y-1">
      <a href="/admin-dashboard" class="mobile-nav-link <%= typeof page !== 'undefined' && page === 'dashboard' ? 'active' : '' %>">
        <i class="fas fa-chart-line mr-3"></i>
        Dashboard
      </a>
      <a href="/add-server" class="mobile-nav-link <%= typeof page !== 'undefined' && page === 'add-server' ? 'active' : '' %>">
        <i class="fas fa-server mr-3"></i>
        Add Server
      </a>
      <a href="/add-service" class="mobile-nav-link <%= typeof page !== 'undefined' && page === 'add-service' ? 'active' : '' %>">
        <i class="fas fa-plus-circle mr-3"></i>
        Add Service
      </a>
      <a href="/connect-api" class="mobile-nav-link <%= typeof page !== 'undefined' && page === 'connect-api' ? 'active' : '' %>">
        <i class="fas fa-plug mr-3"></i>
        Connect API
      </a>
      <a href="/my-services" class="mobile-nav-link <%= typeof page !== 'undefined' && page === 'my-services' ? 'active' : '' %>">
        <i class="fas fa-list mr-3"></i>
        My Services
      </a>
      <a href="/bot-settings" class="mobile-nav-link <%= typeof page !== 'undefined' && page === 'bot-settings' ? 'active' : '' %>">
        <i class="fas fa-cog mr-3"></i>
        Bot Settings
      </a>
      <a href="/qr-code" class="mobile-nav-link <%= typeof page !== 'undefined' && page === 'qr-code' ? 'active' : '' %>">
        <i class="fas fa-qrcode mr-3"></i>
        QR Code
      </a>
    </div>
  </div>
</nav>

<!-- Navbar Styles -->
<style>
  .nav-link {
    @apply px-4 py-2 rounded-xl text-gray-700 hover:text-blue-600 hover:bg-gradient-to-r hover:from-blue-50 hover:to-blue-100 transition-all duration-300 font-medium text-sm relative overflow-hidden;
    /* Prevent horizontal overflow */
    box-sizing: border-box;
    max-width: 100%;
  }
  
  .nav-link::before {
    content: '';
    position: absolute;
    top: 0;
    left: -100%;
    width: 100%;
    height: 100%;
    background: linear-gradient(90deg, transparent, rgba(59, 130, 246, 0.1), transparent);
    transition: left 0.5s;
    /* Ensure the pseudo-element doesn't cause overflow */
    box-sizing: border-box;
  }
  
  .nav-link:hover::before {
    left: 100%;
  }
  
  .nav-link.active {
    @apply bg-gradient-to-r from-blue-600 to-blue-700 text-white hover:from-blue-700 hover:to-blue-800 shadow-lg;
  }
  
  .mobile-nav-link {
    @apply block px-4 py-3 rounded-xl text-base font-medium text-gray-700 hover:text-blue-600 hover:bg-gradient-to-r hover:from-blue-50 hover:to-blue-100 transition-all duration-300;
    /* Prevent horizontal overflow */
    box-sizing: border-box;
    max-width: 100%;
  }
  
  .mobile-nav-link.active {
    @apply bg-gradient-to-r from-blue-600 to-blue-700 text-white hover:from-blue-700 hover:to-blue-800;
  }
  
  /* Ensure the navbar container doesn't cause horizontal scroll */
  .navbar-container {
    width: 100%;
    max-width: 100vw;
    overflow-x: hidden;
  }
</style>

<!-- Mobile Menu JavaScript -->
<script>
  document.addEventListener('DOMContentLoaded', function() {
    const mobileMenuButton = document.getElementById('mobile-menu-button');
    const mobileMenu = document.getElementById('mobile-menu');
    
    mobileMenuButton.addEventListener('click', function() {
      mobileMenu.classList.toggle('hidden');
    });
    
    // Close mobile menu when clicking outside
    document.addEventListener('click', function(event) {
      if (!mobileMenuButton.contains(event.target) && !mobileMenu.contains(event.target)) {
        mobileMenu.classList.add('hidden');
      }
    });
  });
</script>
